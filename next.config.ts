/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // Se houver outras configurações existentes, mantenha-as abaixo
};

module.exports = nextConfig;